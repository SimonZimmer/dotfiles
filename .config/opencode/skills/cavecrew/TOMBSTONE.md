Removed intentionally — do not reinstall.

No SKILL.md here on purpose, so opencode won't register this as a skill. Directory exists only so `caveman` installer's exists-check (skip-unless---force) doesn't recopy the cavecrew skill on update. Caveman mode already applies globally via ~/.config/opencode/AGENTS.md + experimental.caveman_default_level.
